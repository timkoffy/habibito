#include "stdio.h"

/*
    todo: 
    save shell state to return previous state after exiting program
    resizing. also real time resizing why not
    parse config file
    ask user to set up config on first execution
    custom art in top-left corner
    flexible width of habit's names column
    storing data in binary file
*/

int main(int argc, char *argv[])
{
    

    return EXIT_SUCCESS;
}
